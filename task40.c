//work on each digit of a number

#include<stdio.h>
void main()
{
	int num,sum=0,rem;
	printf("enter a 3 didgit number :");
	scanf("%d",&num);
	
	
/*	rem=num%10;
	sum=rem+sum;
	num=num/10;
		rem=num%10;
	sum=rem+sum;
	num=num/10;
		rem=num%10;
	sum=rem+sum;
	num=num/10;
	printf("%d",sum);*/
}
